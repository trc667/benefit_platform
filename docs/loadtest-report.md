# 压测报告（`node loadtest/run.mjs --all` 自动生成）

> 生成时间：2026/9/10 14:41:28　目标：`http://127.0.0.1:8090`
>
> 工具：`loadtest/` 自研压测器（Node 内置 http + keep-alive 连接池，零依赖）。
> 说明：压测机与后端**同机回环**，数字用于「趋势对比」而不是「生产容量承诺」；
> 「非预期错误率」只统计网络/HTTP 失败与业务码 500，**429 限流、6002 售罄、2001 今日已签到属于预期内的明确拒绝**。

## 1. 结论速览

| 场景 | 请求数 | 吞吐(req/s) | P95(ms) | P99(ms) | 非预期错误率 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| 签到高峰（200 用户×2 并发） | 400 | 982.8 | 374.9 | 387.2 | 0% | 异步入账延迟 P95 289ms |
| 领券秒杀（200 并发抢 100 张） | 200 | 546.4 | 347.4 | 358.2 | 0% | 成功 100 / 库存 100，零超发 |
| 结算页算价（CompletableFuture 并行） | 26165 | 1743.8 | 68 | 79.9 | 0% | 商品 #2 |
| 积分账户热点写（60 并发写同一账户） | 60 | 107.7 | 529.9 | 555.9 | 0% | 成功 60/60，余额增量与流水一致 |
| 并发下单支付（200 并发） | 200 | 185.9 | 1059.9 | 1072.7 | 0% | 成功 200 单，扣分 30000，库存 -200 |

## 2. 阶梯压测（找容量拐点）

接口：`GET /api/order/settle/preview`（幂等只读，适合持续压）

| 并发 | 吞吐(req/s) | 成功(req/s) | P50(ms) | P95(ms) | P99(ms) | 非预期错误率 |
| --- | --- | --- | --- | --- | --- | --- |
| 50 | 1780.9 | 1780.9 | 26.9 | 32.8 | 41.6 | 0% |
| 100 | 1766.8 | 1766.8 | 53.9 | 65.1 | 101.3 | 0% |
| 200 | 1778.8 | 1778.8 | 108.3 | 133.8 | 205.5 | 0% |
| 400 | 1733.5 | 1733.5 | 224.6 | 261.3 | 503.7 | 0% |

**观察**：吞吐在 **1780.9 req/s** 见顶（50 并发），把并发从 50 抬到 400（8 倍），
吞吐只涨了 -2.7%，而 P99 从 41.6ms 涨到 503.7ms（12.1 倍），全程 0 错误。
这是典型的**吞吐饱和 + 延迟堆积**：瓶颈在服务端处理能力（或与后端同机的压测客户端），而不是应用错误。
下一步定位方向：JVM CPU 火焰图 → Hikari 等待线程数 → Redis 单线程命令耗时。

## 3. 并发正确性断言

压测不只测「快不快」，更要证「**对不对**」：下面每一条都是跑完压测后，通过接口/数据库核对得出的结论。

| 断言 | 结果 | 说明 |
| --- | --- | --- |
| 并发签到不会重复成功（每用户成功次数 ≤ 1） | ✅ 通过 | 200 人成功，199 次被分布式锁挡下，1 次被判今日已签到 |
| 相同用户并发请求全部被明确业务码拒绝，无 5xx | ✅ 通过 | 拒绝都是 429/2001，不是 500 |
| 成功数 ≈ 用户数（没有因并发竞争丢失签到） | ✅ 通过 | 200/200 用户签到成功 |
| 积分只入账一次（异步链路无重复消费） | ✅ 通过 | 抽查 30 人：流水不为 1 条的 0 人 |
| 余额增量 == 流水金额（无静默丢分） | ✅ 通过 | 30/30 人匹配 |
| 发放数恰好等于库存（100 张，不超发） | ✅ 通过 | 实际成功 100，售罄拦截 100 |
| 券模板 issued_count 与成功数一致 | ✅ 通过 | issued_count=100 |
| 无 5xx / 网络错误 | ✅ 通过 |  |
| 单人限领未被突破（抽查 10 人） | ✅ 通过 |  |
| 无 5xx / 网络错误 | ✅ 通过 |  |
| P99 < 500ms | ✅ 通过 | 实际 79.9ms |
| 成功次数与余额增量一致（没有静默丢积分） | ✅ 通过 | 初始 340 + 成功 60×10 = 940，实际 940 |
| 余额不为负 | ✅ 通过 | 余额 940 |
| 热点账户并发写不出现系统错误码 500 | ✅ 通过 | 全部请求都有明确结果（成功 60） |
| 无网络/HTTP 层失败 | ✅ 通过 | 无失败 |
| 没有任何账户被扣成负数（不超扣） | ✅ 通过 |  |
| 扣减总额 == 成功订单 × 单价 | ✅ 通过 | 扣减 30000，成功 200 单 × 150 |
| 库存扣减与成功订单一致 | ✅ 通过 | 库存 378 → 178，成功 200 单 |
| 找到吞吐拐点（至少一级保持 99% 成功率） | ✅ 通过 | 拐点在 400 并发附近（RPS 1733.5，P99 503.7ms） |

> 原始数据：`loadtest/results/2026-09-10T06-41-02-129Z.json`
